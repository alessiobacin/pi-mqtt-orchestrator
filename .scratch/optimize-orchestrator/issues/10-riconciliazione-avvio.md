## Question

Riconciliazione all'avvio (sezione 27) resa reale e TESTATA, sul pattern
firstmate: coda durevole di eventi + lock singleton + generazione/ack (un evento
vecchio non viene mai riapplicato due volte). Mappato su SQLite + retained MQTT.

Questa è la priorità alta #4 della comparazione. Decisione sul formato
(coda+lock+generazione) e sulla copertura di test richiesta prima di chiudere.

Type: task
Blocked by:
Status:
